VIDEO EVIDENCE WORKBENCH progress log

2026-09-04 v0.2.1 maintenance candidate
- Corrected premature completion: the task was reopened because five Dependabot PRs remained unhandled and v0.2.0 did not contain the post-release receipt-gate repair.
- Added a tested npm-audit result classifier. Confirmed high/critical findings, malformed counts and unknown errors fail; only an explicitly identified npm advisory endpoint timeout/503 is reported as unavailable, never as a clean audit.
- Rebased and fully verified GitHub Actions PRs #2/#3/#4/#5 in cumulative order, then merged checkout, upload-artifact, setup-python and setup-node v7. Each exact PR SHA passed frontend, security, minimum dependency, Linux/macOS Python 3.11/3.12 and real XLSX/PDF/browser jobs.
- Closed grouped frontend PR #6 without merge after independent review found stale packaged assets and UI acceptance evidence. Dependabot now groups frontend minor/patch versions only so major upgrades can be reviewed separately.
- Updated Python, frontend, lock, MCP client, citation, README, changelog and release-note metadata to 0.2.1 and added an automated consistency contract.
- Fresh local gates: 520 passed, 29 skipped, 393 subtests in 47.32 s; Ruff/Bandit clean; pip-audit and npm audit found no known vulnerability; frontend integration/PDF diagnostics/build/E2E passed; real XLSX/PDF/browser 71/71 passed in 63.015 s; all workflow smokes passed; video benchmark 6/6 functional and 5/5 accuracy-gated in 5.646 s; audio benchmark 5/5 in 0.071 s; 215-file clean install/migration/wheel/served-asset parity passed.
- Frozen product digest independently matched 210 files at `3f7bd9ad96e941afb78aafb48961f5acb031234d2bdb6863f79c2472c1a56681`. Final cold review approved with no remaining P0-P3 after the old v0.2.0 verdict was explicitly moved under history and v0.2.1 received its own verdict contract.
- Exact PR CI, immutable v0.2.1 tag CI and GitHub pre-release publication remain mandatory before completion.

2026-07-15 product reset
- Reframed the project from an internal ad-delivery operator into an open-source, local-first, auditable shot-level video research workbench.
- Removed the production UI's silent mock fallback, fixed project-scoped navigation, and replaced the canvas/chat-first surface with player, shot strip, inspector, table, readiness, and evidence deliverables.
- Unified the React build, API, and project files behind one localhost server while preserving an explicit legacy fallback.
- Added deterministic `reports/codex_handoff.md` and `data/visualization_dataset.json` artifacts with project-relative evidence references and unverified-item disclosure.
- Removed mock order/payment routes and stopped exposing simulated agent/image-generation routes from the public API.
- Added the open-source release surface, product strategy, Codex companion documentation, SEO/GEO assets, CI, tests, and responsive design concepts.
- The 2000-star objective is a growth scenario, not a quality claim or guaranteed outcome. Remaining gates live in `prd.json`.

2026-07-15 superseded verification receipt
- An earlier 65-test checkpoint passed after the first security, provenance, profile, optional FastAPI, provider-receipt, accessibility-contract, doctor-diagnostics, and documentation convergence pass. Later hardening expanded this suite to 133 tests; both counts are historical and superseded by the 2026-07-21 receipt below.
- The earlier minimum-dependency claim was invalidated during final resolver verification: FastAPI 0.139.0 requires Pydantic 2.9.0 or newer. The declared Pydantic floor is now 2.9.0 and must pass the final isolated matrix before release.
- Production frontend: TypeScript + Vite build passed; the resulting assets are bundled in the Python package.
- Synthetic package smoke, real-media API smoke with CSRF, and a clean-source installed-wheel React serving check passed. The install check also rejects unreferenced frontend assets in the wheel.
- npm audit reported zero vulnerabilities. Python runtime dependency auditing against the corrected Pydantic 2.9.0 and yt-dlp 2026.6.9 minimums remained a later release gate at this checkpoint.
- Bandit reported zero medium- or high-severity findings after endpoint validation was repeated at the network boundary and the optional MiniMax MCP package was pinned.
- Desktop 1600px and mobile 390px viewport checks passed on the local sample workspace.
- Three pre-fix repository reviews failed and drove security, readiness, profile, provider-receipt, and touch-target fixes. A separate provenance/UI lane passed cold review; the final repository-wide cold review runs only after the corrected tree is frozen.

Still unverified
- Live OpenAI and MiniMax provider behavior, cost, retention, and regional availability.
- Remote GitHub Actions execution of the declared macOS/Linux matrix, plus all Windows behavior.
- A licensed public sample package and multi-codec benchmark.
- Canonical GitHub URL, repository topics, social preview, releases, and measured adoption funnel.
- A public real-product screenshot generated from the new persistent demo; the README concept is explicitly labeled as a concept instead.

2026-07-15 open-source activation hardening
- Added `scripts/run-demo.sh`: a one-command, persistent four-second synthetic-media run through the real research pipeline with ASR skipped, provider keys unset, and no network download. It writes a new ignored project, refuses collisions, and emits `demo-receipt/v1` plus key artifact paths.
- Added the small `examples/demo/expected-receipt.json` contract and a CI smoke that verifies a reported manifest, fail-closed readiness, no vision receipt, and non-empty shot/storyboard/Codex/visualization artifacts.
- Local prepared-environment timing was 1.31 seconds; this is not a clean-install or cross-platform benchmark. The prepared-demo target is five minutes and the clean-start target remains ten minutes.
- Expanded the declared CI matrix to Ubuntu/macOS and Python 3.11/3.12, while keeping Windows explicitly unverified. All GitHub Actions are pinned to full commit SHAs with version comments, and Dependabot is configured for Actions updates.
- Reconciled README UI mutation language, conditional security reporting, OS classifiers, Codex product naming, OpenAI model/frame/cost/retention boundaries, canonical/SEO blocking, and the non-guaranteed 24-month star funnel with formulas, definitions, data sources, roles, and quarterly checkpoints.

2026-07-15 activation verification receipt
- Python suite and compile check at this checkpoint: 133/133 passed.
- Persistent demo and demo contract smoke passed; an explicit repeat project ID was rejected without overwriting the existing receipt.
- Synthetic package smoke, real-media API smoke, frontend drift-gate self-test, and clean-candidate installed-wheel/frontend serving smoke passed.
- The clean install smoke rebuilt 1,699 frontend modules, reported zero npm vulnerabilities, and matched all four packaged frontend files byte-for-byte.
- Workflow, Dependabot, CFF, PRD, expected receipt, shell syntax, and local Markdown links parsed successfully.
- Action pins were read back from the official repositories: `actions/checkout` v4.3.1 (`34e114876b0b11c390a56381ad16ebd13914f8d5`), `actions/setup-python` v5.6.0 (`a26af69be951a213d495a4c3e4e4022e16d87065`), and `actions/setup-node` v4.4.0 (`49933ea5288caeca8642d1e84afbd3f7d6820020`).

2026-07-21 superseded first-candidate verification receipt
- Core-only Python 3.14 environment: compile passed and 174 unittest entries passed with two explicit optional-API skips. The collection contract was corrected so a base install no longer fails when the `api` extra is absent.
- Exact declared-minimum environment: Pillow 12.3.0, Pydantic 2.9.0, yt-dlp 2026.6.9, FastAPI 0.139.0, Starlette 1.3.1, uvicorn 0.51.0, python-multipart 0.0.31, and setuptools 83.0.0; `pip check`, compile, and all 181 tests passed.
- Package, persistent-demo, real-media API, and frontend drift-gate smokes passed. The clean-candidate install smoke copied 99 release files, rebuilt 1,699 frontend modules, found zero npm vulnerabilities, and matched source build, packaged mirror, wheel, and served frontend bytes across all four current assets.
- Ruff 0.15.22 passed `src` and `tests`; Bandit 1.9.4 reported no medium/high source findings; pip-audit 2.10.1 found no known vulnerability in the installed minimum-dependency environment (the unpublished editable project itself is not present on PyPI and was skipped).
- The report manifest now publishes a UUID-bound SHA-256 generation receipt last and fails professional deliverables closed on partial, stale, aliased, missing, or modified artifacts. Visual extraction stages its managed files and commits its digest marker last with rollback on commit failure.
- Independent repository-wide product, code, and security cold review subsequently rejected this candidate. The verification commands above remain historical evidence, not a release approval.

2026-07-21 second-candidate remediation receipt
- Code review blockers were repaired with lock-linearized report verification, strict visual/audio generation receipts, marker-last multi-file commits with rollback, and descriptor/stat-fingerprint caches that avoid repeated media hashing and image decoding. Report generations now bind both exact source generations.
- Security review blockers were repaired with exact same-origin loopback CORS, descriptor-open project file serving under the shared generation lock, browser/API local-file-only intake, initial non-public URL rejection for trusted CLI downloads, private yt-dlp URL/password files, bounded subprocess output with process-group cleanup, and dynamically sized Markdown fences for untrusted draft content. Downloader redirects and DNS rebinding remain an explicit trusted-CLI boundary.
- Product review blockers were repaired with a single lock-consistent `/workspace` snapshot and digest, verified report generation ID, provider-receipt/human/unverified state separation, English-first API/UI labels, exact navigation states, and explicit available/blocked/missing deliverable actions.
- Exact declared-minimum environment: `pip check`, compile, and 213 unittest entries passed. The core Python 3.14 environment passed 203 entries with two explicit optional-FastAPI skips. Ruff and Bandit passed; pip-audit found no known vulnerability in the installed minimum dependency set and skipped only this unpublished editable package.
- Package, persistent-demo, real-media API, and frontend drift-gate smokes passed after the hand-authored package smoke was upgraded to create real visual/audio receipts. The clean-candidate install smoke copied 100 release files, rebuilt 1,699 frontend modules, reported zero npm vulnerabilities, and matched source build, packaged mirror, wheel, and served frontend bytes across all four current assets.
- `VEW-004` remains `passes: false`: source/build/contract checks are current, but repository-stored production desktop/mobile screenshot receipts are still absent. A fresh exact-candidate browser cold review is required before any responsive-release claim.

2026-07-21 third-candidate remediation receipt
- The second exact candidate was rejected after independent review reproduced a reverse-order `project`/`shots` lock deadlock, a report-preview authorization/read race, a MiniMax descendant process-group leak after leader exit, incomplete component-level URL redaction, and a 761–900px evidence-table gap. The candidate was thawed; it is not a release approval.
- All paths needing both workspace locks now acquire `project` before `shots`, with a regression contract covering both workspace snapshots and legacy shot edits. Text deliverable generation verification, readiness authorization, and descriptor-backed preview reads now share one shots transaction; a controlled concurrent publisher test proves the preview cannot read replacement bytes authorized against the previous generation.
- MiniMax pipe draining uses one bounded shared grace interval and still terminates the POSIX process group after its leader exits. URL error redaction now covers raw and decoded query keys, values, and fragments even when an external tool echoes a component without the complete URL. Local marker and percent-encoded component regressions pass.
- The 900px layout retains the cross-shot evidence table without page-level horizontal overflow, while the compact mobile export view visibly preserves Available, Blocked, and Missing states and keeps unavailable actions disabled. Fresh Chrome checks at 900x1000 and 390x844 reported no console warnings/errors or overlaps; screenshots remain temporary review evidence, not repository release receipts.
- The isolated declared-dependency environment passed `pip check`, compile, and 218 unittest entries. The system Python 3.14.5 environment passed 208 entries with two optional-FastAPI skips. Ruff 0.15.22 and Bandit 1.9.4 passed. The isolated candidate dependency audit found no known vulnerability; the host's unrelated global/user-site Python environment has separate third-party advisories and is not a candidate dependency receipt.
- Package, persistent-demo, real-media API, and frontend drift-gate smokes passed. The clean-candidate install smoke copied 100 release files, rebuilt 1,699 frontend modules, reported zero npm vulnerabilities, matched the four source/package/wheel assets byte-for-byte, and served every installed asset exactly. `VEW-004` remains false until a real production screenshot receipt is deliberately added to the repository.

2026-07-21 fourth-candidate remediation receipt
- The third exact candidate was rejected after independent review found that a committed report did not bind every shot semantic or the canonical media snapshot, the contributor Vite server exposed proxied API responses to a sibling localhost origin, MiniMax cleanup missed closed-stdio descendants and allowed a blocking stdin write to outlive the declared deadline, and a keeper choice mutated manifest-bound lineage after publication. The candidate was thawed before repair.
- Report receipt schema v3 now verifies the persisted media snapshot and binds a canonical current-readiness payload covering the complete shot digest, media package and current media receipts, frame/visual state, and explicit vision-receipt presence, in addition to the committed visual/audio generations and every published artifact. Keeper choices are stored as a separate operator decision and no longer rewrite committed lineage. Semantic-shot, media, stale-preview, and keeper regressions pass.
- The Vite development and preview servers now disable permissive CORS explicitly. A real programmatic Vite test proves a sibling `localhost` origin receives no `Access-Control-Allow-Origin`, while same-origin requests remain usable and the proxy rewrites Origin and Host to the exact loopback backend. The integration gate runs in CI before the production build.
- MiniMax communication now caps input at 4 MiB, covers stdin writing with the same process deadline, fails closed on BrokenPipe and writer/output errors, and unconditionally performs best-effort POSIX process-group cleanup and leader reaping after normal or abnormal exit. The focused 22-test provider-security suite and an independent POSIX/process cold review approved the boundary; active `setsid()` escape and unreapable-process fault injection remain untested.
- The system Python 3.14.5 environment passed compile and 216 unittest entries with two optional-FastAPI skips. The isolated exact declared-dependency environment passed `pip check`, compile, and all 226 entries. Ruff 0.15.22 and Bandit 1.9.4 passed. The isolated installed dependency set had no known advisories; npm reported zero vulnerabilities.
- Package, persistent-demo, real-media API, frontend drift-gate, and clean-candidate install smokes passed. The clean candidate copied 104 files, rebuilt 1,699 modules, matched the four source/package/wheel frontend files byte-for-byte, and served every installed asset exactly. JSON, TOML, YAML/CFF, shell syntax, local Markdown links, and screenshot receipts also validated.
- Repository-stored desktop and mobile PNG screenshots now show the real production UI with the local synthetic demo and its deliberately blocked/unverified truth; a machine-readable receipt binds the reviewed source freeze, current frontend source tree, served asset bytes, screenshot hashes/dimensions, browser version, fixture, and interaction results. Independent Playwright checks at 1440x900, 900x1000, and 390x844 returned 200 with no console warnings/errors, page-level overflow, visible sub-44px controls, or horizontally out-of-viewport elements; desktop evidence surfaces, all four mobile modes, drawer focus restoration, and Available/Blocked/Missing states passed. `VEW-004` is now true. These files are local candidate evidence, not a public release attestation.
- This is a local candidate receipt, not a public release. Live provider contracts, Windows, a licensed multi-codec benchmark, canonical GitHub metadata, remote CI execution, and measured adoption remain unverified; the 2,000-star goal remains a scenario rather than a claim or guarantee.

2026-07-21 fifth-candidate primary review and finalization receipt
- This entry supersedes the fourth-candidate screenshot/current-file description. The current 108-file Git candidate includes the two Vite-hashed JS/CSS assets through extension allowlists; a clean-candidate build, packaged mirror, wheel, and loopback server matched all four frontend files byte-for-byte after an independent review exposed and closed the stale hashed-asset ignore rule.
- The production React workspace now owns the complete all-shot review loop: optimistic edits, no 24-shot cap, Save & next unresolved, a separate visual-generation-bound low-confidence boundary assertion, and one explicit Finalize action. Stored screenshots show the synthetic fixture after human review and finalization; no external vision provider ran.
- Human-review saves mark the manifest `review_pending`, remove the prior publication commit record, and keep professional export blocked. The real HTTP workflow proves blocked → reviewed but unfinalized → finalized ready → mutation blocked → byte-identical restoration still blocked → second finalization ready; the old professional URL stays denied while invalidated.
- Independent code cold review reproduced a Finalize-time loss of the editable `dialogue` field. Report synthesis now preserves both non-empty and explicitly empty human/provider dialogue decisions while storing transcript-derived text in `speech_summary`; a unit regression and the end-to-end workflow smoke submit and verify non-empty human dialogue.
- Current targeted evidence includes the review workflow smoke, frontend same-origin integration, TypeScript/Vite production build, npm audit with zero high-severity findings, clean-candidate installed-wheel serving, Ruff, Bandit, pip check, and Python dependency audit with no known advisories (the unpublished editable package is skipped). The exact final candidate hash and full-suite readback are recorded only after the receipt freeze.
- Independent security review found no authorization bypass. One P3 self-only transaction caveat remains: a disk failure after `shots.json` but before `boundary_review.json` may return an error after persisting the shot edit; the manifest is already invalidated and export remains blocked, so retry/reload is safe but the write is not a fully atomic multi-file user experience.
- This is a usable local alpha for the documented short-video review job, not a mature cross-platform release. Persistent long-run status/resume, the six-to-twenty-video benchmark, live provider contracts, Windows, remote CI, canonical GitHub publication, and measured adoption remain P1/public-release work. The 2,000-star objective is not guaranteed.

2026-07-21 sixth-candidate process-cleanup hardening receipt
- A declared-minimum Python 3.11 full-suite run exposed one transient macOS `killpg(..., SIGKILL)` `EPERM` while cleaning an output-limited subprocess. Thirteen independent reproductions did not repeat it, so it was classified as a platform race rather than a stable dependency failure, but the cleanup path was hardened instead of waived.
- POSIX group cleanup now preserves the original timeout/output-limit failure when macOS reports `EPERM` and directly kills the process leader when it is still alive. The existing descendant-survival assertion remains intact, and a deterministic regression forces both group signals to fail so this fallback cannot regress silently.
- Storyboard rendering now treats an explicitly empty human/provider dialogue as silence instead of falling back to a machine transcript summary; machine-authored rows retain the useful fallback. A provenance regression covers both behaviors.
- This hardening does not broaden the product promise. It removes a local reliability edge before freezing the current review/finalization candidate; public release gates and the P3 cross-file review transaction caveat remain unchanged.

2026-07-22 local release-candidate lifecycle and benchmark implementation
- Added workspace-local UUID run receipts with queued, running, cancelling, cancelled, completed, failed, and interrupted states; five stage timings; bounded failure details; process-loss recovery; cooperative stage-boundary cancellation; and receipt-verified retry that skips valid media, visual, audio, or report generations.
- Added `/api/runs` create/list/status/retry/cancel routes while preserving synchronous `/api/projects` behavior. The background surface rejects passwords and external vision; provider work remains a separate explicit action.
- Replaced the blocking React creation request with a reload-safe run page containing progress, stage history, timestamps, cancellation, failure detail, safe resume, and a final workspace link. The production bundle remains source-mirrored in the Python package.
- Added a no-network six-case synthetic benchmark covering hard cuts, fade/dissolve, animation, vertical video, variable frame rate, and an audio-dense speech surrogate. Its JSON receipt includes environment, per-stage timing, boundary precision/recall, artifacts, fail-closed readiness, and scope boundaries.
- The first benchmark run exposed FFmpeg 8 MJPEG/tile behavior on short clips and an error-envelope exit-code bug. Contact sheets now sample a complete 3x4 grid with full-range JPEG output, and CLI error envelopes return non-zero.
- The quality contract now separates functional completion from accuracy. Five declared cases are accuracy-gated: hard-cut cases require precision >= 0.8 and recall >= 0.5, and no-cut cases allow no false-positive boundaries. Fade/dissolve is observational, has `passed: null`, and cannot increase the pass count.
- Current prepared-machine benchmark receipt: 6/6 functional and 5/5 accuracy-gated cases passed in 5.823 seconds; hard-cut and vertical-hard-cut precision/recall were 1.0/1.0; every case remained blocked before human review. Fade/dissolve recall was 0.0 and remains an explicit observed limitation, not a claimed accuracy pass.
- Cold review reproduced cross-run project reuse and concurrent publication risks. The lifecycle now holds a whole-pipeline cross-process project lease, binds one project to one run, rejects existing projects for new runs, verifies source/profile on retry, and writes final generation bindings into completed receipts.
- Final local gates now include real synchronous and asynchronous HTTP smoke, clean wheel/install serving, exact minimum dependency checks, production frontend build and same-origin integration, Ruff, Bandit, Python/npm advisory audits, and desktop/tablet/mobile browser reload/overflow/accessibility checks. The refreshed browser screenshots use the final packaged JavaScript candidate.
- Independent final cold review reproduced the prior failure modes and found no remaining P0/P1 in code: project collision is fail-closed and permanently non-retriable, an interrupted owner commit recovers only an exact empty managed tree, concurrent CSRF acquisition has isolated deadlines, and the benchmark aggregate cannot count the observational case as passed.
- The hash-bound UI acceptance receipt is the final candidate freeze. A full-suite readback after that self-referential exclusion is required before the local RC can be reported as verified; public release, Windows, remote CI, live providers, and adoption remain outside this receipt.

2026-09-04 mature local-candidate verification
- Completed the structured VO/music/SFX timeline, operator review, readiness integration, professional on-demand XLSX/PDF, explicit current/saved export center, migration/recovery, Codex current-task contract, BridgeDeck adapter boundary, resource limits, benchmarks, open-source documentation and launch measurement plan.
- Rejected over-correction: no automatic export/Finalize/migration, no heavyweight model/browser in the base install, no duplicate transaction framework, no credential discovery, no fake provider/model/human verification, and no invented GitHub identity or 2,000-star claim.
- A standard Codex Security scan found three medium and two low issues on its baseline snapshot. Current repairs closed ffprobe timeout, CLI secret argv, FastAPI cross-site fetch, private-value-file TOCTOU, playlist amplification, and verified-tool path drift. yt-dlp redirect/DNS rebinding remains an explicit trusted-operator URL boundary; provider total-response deadline remains a low deferred item.
- Current full Python suite: 511 passed, 28 optional-runtime skips and 391 subtests. Ruff, Bandit, uv lock, Python dependency audit and npm audit pass; browserslist was updated from 4.28.6 to 4.28.8 after current advisories appeared.
- Current real-render matrix: 69/69 passed with Playwright/Chrome, Noto CJK, openpyxl, pypdf and LibreOffice. The first local attempt used a font without complete simplified-Chinese coverage and correctly failed before publication; the correct Noto font passed without weakening validation.
- Fresh install, installed migration, package/demo/API/review smokes, six-video benchmark, five-case PCM benchmark, frontend integration/build/E2E and artifact cleanup pass. Install and browser temporary workspaces were removed; no test browser/service remains.
- Six current screenshots cover desktop 1440x900, tablet 900x1000 and mobile 390x844, including review drawer, current XLSX export and persistent run. The UI receipt binds the 202-file product candidate at `ad44b66a1ffe1c1ae6708718000cda50e5e03300c9514e2a31db1c059bd6143d` while excluding self-referential evidence files that are hashed separately.
- Final independent mature-candidate cold review remains the last local gate. Public GitHub upstream, remote CI, Windows, live providers, real ASR/semantic sound accuracy, public adoption and 2,000 stars remain unverified.
