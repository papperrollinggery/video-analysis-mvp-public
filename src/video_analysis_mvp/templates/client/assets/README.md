# Template assets

The default client template intentionally ships without a logo or remote image.
Validated user branding is supplied only during an explicit export operation and
is bound in that export receipt. Renderers must not download assets at runtime.
